const require_index = require("../clients/index.cjs");
const require_base = require("./base.cjs");
let _langchain_core_utils_env = require("@langchain/core/utils/env");
//#region src/chat_models/index.ts
var ChatGoogle = class extends require_base.BaseChatGoogle {
	apiKey;
	constructor(modelOrParams, paramsArg) {
		const params = require_base.getGoogleChatModelParams(modelOrParams, paramsArg);
		params.apiKey = params?.apiKey ?? (0, _langchain_core_utils_env.getEnvironmentVariable)("GOOGLE_API_KEY");
		const apiClient = params?.apiClient ?? new require_index.WebApiClient(params);
		super({
			...params,
			apiClient
		});
		this.apiKey = params.apiKey;
	}
};
//#endregion
exports.ChatGoogle = ChatGoogle;

//# sourceMappingURL=index.cjs.map